# TODO List

## 高优先级

- 修复和统一文本编码：部分历史中文文案在文件中已经出现乱码，应逐步替换为正常 UTF-8 文案。
- 拆分 `public/js/app.js`：建议按 `auth`、`lobby`、`game`、`settlement`、`inventory`、`config` 分模块。
- 拆分 `src/server.js`：建议按 `auth`、`rooms`、`game-engine`、`loot-generator`、`accounts`、`admin-api` 分模块。
- 给房间快照增加 `version` 或 `updatedAt` 增量机制，未变化时返回更轻量的数据。
- 增加多人手动测试脚本，覆盖创建房间、选择角色/道具、完整竞拍、清点、入库、离房销毁。
- 完善后台校验：保存 `loot-config.json` 前检查 id 重复、尺寸范围、图片路径、地图权重和门票金额。

## 中优先级

- 将 HTTP 轮询升级为 WebSocket 或 SSE，减少延迟和重复请求。
- 给角色和道具增加配置文件，避免前端和服务端各维护一份常量。
- 增加房间准备状态：所有玩家准备后房主才能开始。
- 增加断线重连提示：玩家重连时清楚显示是否已被 AI 接管。
- 增加局内道具音效和角色语音配置表。
- 增加局内操作回放或事件日志导出，方便排查好友局争议。
- 增加账号数据备份/恢复入口，避免误删 `data/accounts.json`。
- 优化移动端布局，尤其是出价键盘、仓库滚动和清点页。

## 玩法扩展

- 增加更多角色：经济型、干扰型、反侦察型、风险转移型。
- 增加更多道具：反扫描、锁定藏品、伪造稀有度、保险、贷款、一次性撤回报价。
- 增加地图特殊规则：高门票高波动、低门票高垃圾率、指定品类加权、隐藏陷阱物品。
- 增加藏品套装系统：同类藏品凑齐后在仓库展示架提供额外收藏价值。
- 增加拍卖节奏配置：限时出价、盲拍、保留价、房主自定义轮数。
- 增加玩家战绩：总盈利、最高单局收益、收藏品估值、亏损次数。

## 美术和音频

- 替换 `public/assets/characters/` 下的角色占位立绘。
- 替换 `public/assets/audio/roles/` 下的角色语音。
- 替换 `public/assets/audio/events/` 下的成交、盈利、亏损音效。
- 给 `config/loot-config.json` 中的藏品逐步配置真实 PNG 贴图。
- 增加 1x1 到 5x5 以外尺寸的占位图生成脚本，方便未来扩展尺寸。

## 工程质量

- 增加 ESLint 或 Biome，统一格式和潜在错误检查。
- 增加单元测试：仓库生成、道具效果、角色技能、结算金额、房间生命周期。
- 增加 API 集成测试：鉴权、创建房间、加入房间、离开房间、销毁房间。
- 把账号文件写入改成原子写入，降低异常退出导致 JSON 损坏的风险。
- 增加配置版本号和迁移逻辑，避免旧配置字段导致启动失败。
- 增加 `KWA_DATA_DIR` 使用说明和启动器可配置入口。
