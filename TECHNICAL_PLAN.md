# 仓库拍卖夜：好友娱乐版技术计划书

## 当前执行补充

- 结算流程改为“逐件揭晓 -> 赢家选择上锁 -> 确认结算”。红色、金色藏品自动上锁；未上锁藏品自动出售；只有上锁藏品持久化进入账户仓库。
- 个人仓库移除一键出售入口，避免误卖；后续批量整理应另做二次确认或回收箱。
- 后台配置页增加前端草稿缓存，轮询刷新不会覆盖正在编辑的表单，保存时一次写入配置文件。
- 暗仓生成取消固定上下限，只保留 15 列；行数由抽到的藏品和紧密排布算法自动扩展。
- 结算完成后的导航拆成进入仓库、继续下一局、返回大厅；返回大厅会把房间放回 lobby，已结束未继续的房间 5 分钟后清理。
- 后台新增 `config/loot-config.json` 可视化 JSON 编辑区，支持地图、藏品和暗仓模板分块草稿编辑并统一保存。
- 藏品标签改为中文标签，并新增 `image` 字段；贴图目录固定为 `public/assets/item-textures/`，没有贴图时使用 `placeholder.png`。

## 1. 目标定位

本项目目标不是上架 Steam 或商业化，而是做成一个“朋友打开浏览器即可游玩”的轻量多人派对游戏。

核心体验：

- 固定 4 人参与一局拍卖。
- 一局围绕 1 个暗仓展开，默认经过 6 轮竞价，可在后台配置。
- 每轮玩家可以使用 1 次角色技能和 1 次道具，获得部分仓库信息。
- 每轮提交报价后公开本轮报价，但不结算。
- 最终轮后最高价得仓库，进入逐件清点和盈亏统计。
- 游戏重点是信息差、心理博弈、抬价、误判和结算反转。

优先级原则：

1. 浏览器可玩优先。
2. 局域网/好友房优先。
3. 少依赖、低部署难度优先。
4. 玩法完整性优先于商业系统。

## 2. 推荐技术路线

### 当前阶段：继续使用 Web

当前原型是原生 HTML/CSS/JavaScript，适合继续扩展。

短期不建议迁移 Unity/Godot，因为目标是浏览器打开即玩，且游戏主体是 UI、格子仓库、竞价和信息揭示。Web 的迭代速度更快。

### 新增技术栈

推荐新增一个零依赖 Node.js 本地服务器：

- 用于托管网页，朋友访问同一个地址即可进入。
- 用于维护房间、玩家、报价、技能、道具、结算状态。
- 初期用 HTTP 轮询即可，后续再升级 WebSocket。

暂不引入：

- 数据库：好友娱乐版先用内存房间。
- 登录系统：用昵称加入即可。
- 复杂构建工具：先保持直接打开/直接启动。

未来可选：

- WebSocket：实时同步更丝滑。
- SQLite：需要历史战绩、收藏存档时再加。
- Vite/React/Vue：当 UI 复杂到原生 DOM 难维护时再迁。

## 3. 架构规划

### 阶段 A：单机垂直切片

当前已基本具备：

- 4 名玩家 UI。
- 默认 6 轮报价流程，可配置。
- 每轮角色技能和道具。
- 暗仓总体轮廓。
- 技能/道具揭示部分信息。
- 最终轮后逐件清点结算。

下一步需要补：

- 更清晰的状态提示。
- 更合理的技能/道具效果。
- 更多仓库模板和藏品数据。
- 对局结束后的再开一局流程。

### 阶段 B：局域网好友房

目标：一人开服务器，朋友访问 `http://主机IP:3000` 加入。

待办：

- 本地服务器托管静态页面。
- 创建房间/加入房间。
- 固定 4 个座位。
- 玩家昵称。
- 房主开始游戏。
- 服务端维护对局状态。
- 客户端轮询房间状态。
- 玩家操作通过 API 提交。

### 阶段 C：完整好友娱乐版

待办：

- 角色选择。
- 道具选择/购买。
- 每轮操作阶段：技能、道具、报价。
- 所有玩家提交后自动开标。
- 超时自动报价或跳过。
- 断线重连。
- 观战或 AI 补位。
- 对局日志。
- 结算清点动画同步。

### 阶段 D：打磨版

待办：

- 更多角色：信息型、扰乱型、经济型、心理型。
- 更多道具：扫描、误导、保险、贷款、封锁、反侦察。
- 仓库生成器：根据稀有度、占格、价值区间自动生成。
- 音效和动效。
- 移动端适配。
- 简单存档：本地收藏册、胜负记录。
- 可配置房间规则：轮数、初始资金、AI 数量。

## 4. 服务端状态设计

一个房间包含：

- `id`：房间号。
- `phase`：`lobby`、`bidding`、`settlement`、`finished`。
- `players`：最多 4 人。
- `lot`：本局仓库。
- `round`：当前第几轮。
- `roundActions`：每位玩家本轮技能/道具/报价。
- `bidHistory`：所有玩家每轮报价。
- `intelLog`：公开信息日志。
- `settlement`：清点进度。

服务端原则：

- 报价、技能、道具、结算都由服务端判定。
- 客户端只发送意图，不直接改对局结果。
- 每个 API 返回房间快照，前端按快照渲染。

## 5. API 草案

- `GET /api/health`：服务器状态。
- `POST /api/rooms`：创建房间。
- `GET /api/rooms`：查看房间列表。
- `POST /api/rooms/:id/join`：加入房间。
- `GET /api/rooms/:id`：获取房间状态。
- `POST /api/rooms/:id/action`：提交操作。

初期操作类型：

- `startGame`
- `useSkill`
- `useTool`
- `submitBid`
- `continueSettlement`
- `resetRoom`

## 6. 当前执行计划

第一轮执行：

- 已完成：新增 `src/server.js`，提供静态页面和房间 API。
- 已完成：新增 `package.json`，用 `npm start` 启动。
- 已完成：新增 `README.md`，说明如何让朋友加入。
- 已完成：前端可检测服务器、创建房间、加入房间并轮询状态。
- 已完成：服务端可维护可配置轮数竞价、技能/道具、AI 补位和自动清点。

第二轮执行：

- 已完成：基础大厅 UI，支持创建房间、输入昵称、加入座位、开始对局。
- 已完成：前端接入 `/api/rooms`，在线时按房间快照渲染。
- 已完成：服务端维护 4 人座位，未满座用 AI 补位。
- 待完善：房主权限、准备状态、离开房间、踢出/换座位。

第三轮执行：

- 已完成：核心对局状态迁移到服务端。
- 已完成：客户端在线时提交操作并渲染服务端快照。
- 已完成：平局重开本轮出价，技能/道具揭示和本轮未开标报价按玩家视角隔离。
- 已完成：玩家停止轮询约 10 秒后，服务端将其转为 AI 接管并继续对局。
- 待完善：多人同时操作的更明显提示、断线重连体验。

第四轮执行：

- 已完成：新增可视化后台 `/admin.html`，用于查看服务状态、局域网地址、房间列表、玩家状态、报价历史和结算进度。
- 已完成：新增 Windows/Linux/macOS 启动器，一键开服并打开后台。
- 已完成：金额系统改为完整金额显示，最高 999,999,999，不再使用 K 单位，手动出价不按步进取整。
- 已完成：后台可修改竞价轮数、初始资金、最高出价、按钮增减金额、暗仓随机行数范围、目标填充率、技能揭示件数、断线接管秒数、结算速度；暗仓列数固定 15。
- 待完善：准备状态、房主权限、超时读秒、断线重连和更完整的 AI 托管设置。

第五轮执行：

- 已完成：新增 `config/loot-config.json`，藏品池、稀有度说明、字段说明和仓库模板集中配置。
- 已完成：服务端和前端读取同一份藏品配置，暗仓按标签从藏品池生成。
- 已完成：暗仓装箱改为多策略随机排列，并从多次结果中选择更高填充率的布局；后续改为唯一藏品抽取，不再复制藏品填仓。
- 已完成：补充多种尺寸与稀有度藏品，包括 1x1 小件、长条件、大型 4x4/5x2/2x5 等。
- 已完成：移除统一价值倍率，藏品配置里的 `value` 直接作为基础价，并在生成时按随机品相浮动为最终价格。
- 已完成：完善“藏品百科”按钮，支持搜索、稀有度筛选和藏品基础信息查看。
- 已完成：清点阶段改为独立页面，右侧仓库按坐标顺序逐件揭晓，左侧成交价、战利品价值和利润实时变化。
- 已完成：报价区改为数字键盘式交互，支持完整金额输入、倍率成交快捷键、沿用上轮报价；倍率提前成交规则已恢复。
- 已完成：暗仓生成改为地图资源预算制；地图先 roll 本局后台资源值，再按稀有度和品类权重抽唯一藏品，按价格段扣资源，达到预算后停止；随后根据藏品占格生成合适仓库行数并紧密装箱。
- 已完成：地图配置前置到 `config/loot-config.json` 的 `mapSettings`，便于后续手动调整门票、资源表、稀有度权重、品类权重和标签池。
- 已完成：新增五档地图，收益和门票阶梯式提升；隐拍黑箱使用全品类池，不单独 roll 品类。
- 待完善：藏品图片/图标素材、更多地图手工权重、角色和道具对不同标签的专属侦测。

第六轮执行：

- 已完成：新增本地账户系统，支持注册、登录、退出和 HttpOnly Cookie 会话。
- 已完成：在线房间创建、加入、轮询和操作会按账户鉴权，避免用别人的玩家 ID 提交操作。
- 已完成：房间玩家绑定账户，开始对局时从账户读取现金和个人仓库。
- 已完成：清点完成后把赢家支付后的现金、获得的藏品、战绩统计写回 `data/accounts.json`。
- 已完成：大厅新增账户面板和个人仓库查看入口。
- 已完成：个人仓库改为独立页面，包含左侧展示架、右侧无限容量主仓库、藏品上锁和出售。
- 已完成：藏品增加简单材质贴图表现，按金属、纸质、晶体、陶瓷、科技、旧物等风格渲染。
- 已完成：本局盈亏只在清点页展示，不再额外累计为账户资金收益。
- 已完成：开始对局时按地图扣除门票；未拍中藏品的玩家会获得赢家本局盈亏绝对值十分之一的系统补贴。
- 待完善：账户资料修改、找回密码、展示架收益、仓库批量整理、后台账户管理和更细的权限系统。
