Runtime account data lives here.

`accounts.json` stores local friend-room accounts, session records, cash, and inventory items. Do not share it publicly because it contains password hashes and session tokens.
